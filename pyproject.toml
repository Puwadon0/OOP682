[project]
name = "week9"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pyside6>=6.10.1",
]

[tool.uv.workspace]
members = [
    "fastapi-lab",
    "week10",
]
